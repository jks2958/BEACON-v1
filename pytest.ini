[pytest]
testpaths = tests
# tests/ imports conftest helpers directly (synth_upload, requires_network),
# so the tests dir itself has to be importable.
pythonpath = . tests
filterwarnings =
    ignore::PendingDeprecationWarning
    ignore::DeprecationWarning
